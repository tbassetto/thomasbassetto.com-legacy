You need bundler (hence ruby) and surge (hence nodejs).

# Run locally

```shell
gem install bundler
bundle install
make serve
```

# Upgrade

```shell
bundle update
```

# Deploy

```shell
make deploy
```
